// Adapters added in PR 4 (USB topology, power), PR 5 (UCSI), PR 6 (TB chain, display config, EDID),
// and PR 7 (vendor GPU SDKs). See windows/README.md for the per-PR plan.
namespace WhatCable.Windows.Backend;

internal static class BackendAssemblyMarker
{
    public const string Version = "0.0.0-scaffold";
}
