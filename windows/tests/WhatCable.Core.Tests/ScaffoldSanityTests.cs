// Tests ported from Tests/WhatCableCoreTests in PR 2. This placeholder keeps
// the test runner happy until then so the scaffolding CI build is green.
using Xunit;

namespace WhatCable.Core.Tests;

public class ScaffoldSanityTests
{
    [Fact]
    public void Scaffold_Builds()
    {
        Assert.True(true);
    }
}
